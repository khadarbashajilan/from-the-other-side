export function createAlert(sighting) {
  // Log a message to the console indicating that an alert should be sent to ghost hunters in the specified location
  console.log(`Send alert to Ghost Hunters in ${sighting.location}`);
}
